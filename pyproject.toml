[project]
name = "lanta-llm-summarization"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy<2",
    "rank-bm25>=0.2.2",
    "sentence-transformers>=5.5.1",
]
